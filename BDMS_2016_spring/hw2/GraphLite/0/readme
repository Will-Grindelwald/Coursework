GrapgLite就是把图结构在内存中实现了，所以一些传统图问题用这个框架解决起来特简单，比如这个SSSP

PS: 在编程的时候发现，这个框架的一个设计点：当一个顶点收到信息后，若它已经是 voteToHalt，则会被唤醒
所以，我设计的算法是：当一个顶点在一个超步中没有更新最短路径，则会 voteToHalt，且不会发信息
所以在我这个算法中，大量的结点在运行时是 voteToHalt 的，资源占用会小很多
